
export interface FormData {
  title: string;
  description: string;
  event_date: string;
  type: string;
  location: string;
  is_online: boolean;
  max_participants: string;
  registration_deadline: string;
  is_free: boolean;
  ticket_price: string;
  link: string;
}
